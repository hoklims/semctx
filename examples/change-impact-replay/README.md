# change-impact-replay fixture

A small workspace shaped like a real incident: one module mixes live protocol pins with an
analysis-only replay adapter, and live runtime plus decision code import that module.

Used by the `semctx impact diff` tests and by `docs/reference/change-impact.md`. It is not
built or type-checked by the semctx workspace.
