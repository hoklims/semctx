{
  "mcpServers": {
    "semctx": {
      "command": "bun",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/semctx-mcp.js"],
      "env": {
        "SEMCTX_ROOT": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
