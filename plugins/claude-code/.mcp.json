{
  "mcpServers": {
    "semctx": {
      "command": "bun",
      "args": ["--cwd", "${CLAUDE_PLUGIN_ROOT}", "${CLAUDE_PLUGIN_ROOT}/dist/semctx-mcp.js"],
      "env": {
        "SEMCTX_ROOT": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
